var saveUser = (user) => {
    console.log(`Saving the user ${user}`);
}
var loadMessage = (message) => {
    console.log(`Message Loading: ${message}`);
}

module.exports = {
    saveUser,
    loadMessage
}